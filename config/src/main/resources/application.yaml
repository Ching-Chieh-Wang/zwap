server:
  port: 8888

spring:
  config:
    import: optional:classpath:env.properties
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI}
          skipSslValidation: true
          clone-on-start: true